---
run_mode: local+conda
description: Commands run in a Conda environment on the local machine.
placeholders:
  {conda_env}: Conda environment name
  {working_dir}: Local working directory (absolute path)
  {device_constraint}: Full device constraint string (omit line if none)
  {check_device}: yes/no
  {health_check_method}: tensor / smi / none
---

## Device Environment Config
> Auto-generated by device-env-setup. Edit manually or re-run /device-env-setup.

- **Run mode**: local+conda
- **Conda environment**: {conda_env}
- **Working directory**: {working_dir}
- **Device constraint**: {device_constraint}
- **Check device availability**: {check_device}

### Command Execution Templates

**host_cmd_template** (network/git, no conda needed):
```bash
cd {working_dir} && {command}
```

**exec_cmd_template** (pip/python, in conda environment):
```bash
conda run -n {conda_env} bash -c 'cd {working_dir} && {command}'
```
