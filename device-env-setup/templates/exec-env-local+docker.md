---
run_mode: local+docker
description: Commands run in a Docker container on the local machine. Network/git on host, pip/python in container.
placeholders:
  {container_id}: Docker container ID or name
  {working_dir_host}: Host working directory (absolute path)
  {working_dir}: Container working directory (same as host if paths match)
  {device_constraint}: Full device constraint string (omit line if none)
  {check_device}: yes/no
  {health_check_method}: tensor / smi / none
---

## Device Environment Config
> Auto-generated by device-env-setup. Edit manually or re-run /device-env-setup.

- **Run mode**: local+docker
- **Container**: {container_id}
- **Working directory (host)**: {working_dir_host}
- **Working directory (container)**: {working_dir}
- **Device constraint**: {device_constraint}
- **Check device availability**: {check_device}

### Execution Rules

- **Network/git operations** → run on host (host_cmd_template)
- **pip/python/program execution** → run in Docker container (exec_cmd_template)
- **Never** run pip/conda/apt/make on the host

### Command Execution Templates

**host_cmd_template** (network/git, on host):
```bash
cd {working_dir_host} && {command}
```

**exec_cmd_template** (pip/python/programs, in container):
```bash
docker exec {container_id} bash -c "cd {working_dir} && {command}"
```
